import React from "react"

interface Props { }

export default function Home(props: Props) {
    return (
        <div className="Home">
            <h1>Inicio</h1>
            <p>Bem-vindo(a)!</p>
        </div>
    )
}